# PROGRESS

プロジェクト開始

Flappy Bird風の最小プロトタイプを作成

画像素材をassetsフォルダに移動し、Canvas描画に組み込み

原因確認用にCanvas上へ当たり判定枠を表示

プレイヤーの当たり判定を内側に縮小

プレイヤーと障害物の当たり判定を手動オフセットで調整

プレイヤーと障害物画像を生成し直してassets内のPNGを置き換え

新しい画像に合わせて当たり判定定数を調整

デバッグ用の当たり判定枠を通常時は非表示に変更

ジャンプ幅を小さくするためjumpPowerを調整

スペースキーとポインター長押しで短時間の追加上昇を追加

長押しジャンプの差が分かりやすくなるよう調整

長押しジャンプを強ジャンプ速度へ切り替える方式に変更

CONVENTIONS.mdの外部検索ルールを検索ポリシーとして更新

CONVENTIONS.mdに日本語のゲーム開発ルールを追加

Canvasを800x450の横長表示に変更

横長化後のテンポ調整としてobstacleSpeedを変更

開始直後の待ち時間短縮のため初回障害物を内側に生成

スコアに応じて障害物速度が少しずつ上がるよう変更

強ジャンプ機能を削除し通常ジャンプのみに戻す

横長画面向けに障害物速度パラメータを上方調整

次チャット引き継ぎ用にHANDOFF.mdを作成

開発管理用にSTATUS.mdとCODEX_LOG.mdを作成しHANDOFF.mdを整理

CONVENTIONS.mdに中断・再開ルールを追加

プロジェクトフォルダ名をgameに変更し、HTML表示名とHANDOFF.mdのパス参照を更新

速度上昇を細かく体感できるよう指数カーブ方式に変更

スコア1ごとに速度が0.1上がる線形上昇へ変更

現在のFlappy Bird風ゲーム本体をflappy-birdフォルダに移動

障害物速度上昇を5スコアごとの段階制へ変更

速度変化を体感しやすくするため段階ごとの上昇量を1へ変更

開始前画面を追加し、プレイヤー初期位置を少し上へ調整

速度上昇に合わせて土管の出現間隔も段階的に短くするよう変更

土管の初期出現間隔を90へ短縮し、テンポを改善

10点以降のテンポ改善のため土管の出現間隔下限を72へ調整

速度と土管の出現間隔をスコア1ごとに変化する方式へ変更

速度上限を削除し、土管間隔の安全下限を60へ変更

後半の難易度を上げるため土管間隔の下限を42へ変更

序盤の難易度調整として土管の隙間を145へ狭め、出現範囲を広げた

難易度調整フェーズ完了

開始前画面、スコアHUD、ゲームオーバー画面をDOM UIとして改善

Web Audio APIでジャンプ、スコア獲得、ゲームオーバーの効果音を追加

Web Audio APIでゲーム中に流れる軽いループBGMを追加

BGMを低音中心の試作からベース、メロディ、リズムを持つ16ステップループへ変更

800x450の横長Canvas向け背景画像を追加し、ゲーム背景を差し替え

Flappy Bird風ゲームを一度完成扱いにした

スマホ操作向けにタップ表記、タッチ干渉防止、小画面UI調整を追加

GitHub Pages公開準備としてルートindex.htmlと.gitignoreを追加

スマホでUI上のタップが効かない問題と横画面フィットをCSSで修正

スマホ横画面フィットを100vhフォールバックと100dvh対応に変更

スマホ画面に合わせてゲーム領域を最大化するフルスクリーン寄りレイアウトへ変更
